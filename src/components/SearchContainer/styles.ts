import styled from 'styled-components';

interface SearchContainerProps {
  
}

export const SearchContainer = styled.main<SearchContainerProps>`
    height: 100vh;
    width: 100vw;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
`;


